spring:
  profiles:
    active: dev
  application:
    name: jobapplication-service
  cloud:
      config:
        uri: http://localhost:9999

---

spring:
 profiles: testing
 cloud:
   config:
     enabled: false
security:
  basic:
    enabled: false
eureka:
 client:
   enabled: false