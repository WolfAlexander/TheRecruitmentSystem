server:
  port: 9999

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: ${config.git.uri}
          username: ${config.git.user}
          password: ${config.git.pass}
